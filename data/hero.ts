export type HeroSlide = {
  slug: string;
};

// Mock data — image for each banner comes from public/home/hero/<slug>.{png,jpg,jpeg,webp}
export const HERO_SLIDES: HeroSlide[] = [{ slug: "hero-1" }, { slug: "hero-2" }];
